import type {ConstantsType} from './types';

const Constants: ConstantsType = {
  defaultActiveScale: 1.2,
  defaultActiveOpacity: 1,
  perspective: 1000,
  elementHeight: 60,
  restElements: 2,
  decelerationRate: 'normal',
  loopCount: 31,
  defaultRotation: '0deg',
};

export default Constants;
